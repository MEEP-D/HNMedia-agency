# ============================================================
# CẤU HÌNH BACKEND & HỆ THỐNG
# ============================================================
backend:
  name: github
  repo: "MEEP-D/HNMedia-agency"
  branch: "main"
  auth_type: oauth
  base_url: "https://hn-media-agency.vercel.app/api"
  auth_endpoint: auth

media_folder: "public/images"
public_folder: "/images"
local_backend: true
publish_mode: editorial_workflow

site_url: https://hn-media-agency.vercel.app
display_url: https://hn-media-agency.vercel.app

slug:
  encoding: "ascii"
  clean_accents: true
  sanitize_replacement: "-"

# ============================================================
# REUSABLE FIELD – SECTIONS CONFIG
# ============================================================

x-sections_config: &sections_config
  label: Các khối nội dung (Sections)
  name: sections
  widget: list
  collapsed: true
  summary: '{{fields.title}} [{{fields.type}}]'
  fields:
    - label: Loại Section
      name: type
      widget: select
      options:
        - { label: "Lưới (Grid)", value: "grid" }
        - { label: "Bộ sưu tập (Gallery)", value: "gallery" }
        - { label: "Văn bản (Text)", value: "text" }
        - { label: "Kêu gọi hành động (CTA)", value: "cta" }
      default: text

    - { label: Tiêu đề, name: title, widget: string, required: false }
    - { label: Tiêu đề (EN), name: title_en, widget: string, required: false }
    - { label: Mô tả phụ, name: subtitle, widget: text, required: false }
    - { label: Mô tả phụ (EN), name: subtitle_en, widget: text, required: false }

    - label: Hiệu ứng Animation
      name: animation
      widget: object
      collapsed: true
      required: false
      fields:
        - { label: Kiểu, name: type, widget: select, options: [none, fade-in, slide-up, zoom-in, blur-in], default: none }
        - { label: Delay (ms), name: delay, widget: number, default: 0 }
        - { label: Duration (s), name: duration, widget: number, default: 1 }

    - label: Background
      name: background
      widget: object
      collapsed: true
      required: false
      fields:
        - { label: Kiểu nền, name: type, widget: select, options: [none, color, image, gradient], default: none }
        - { label: Màu, name: color, widget: string, required: false }
        - { label: Ảnh nền, name: image, widget: image, required: false }
        - { label: Gradient CSS, name: gradient, widget: string, required: false }

    - { label: Nội dung, name: body, widget: markdown, required: false }
    - { label: Nội dung (EN), name: body_en, widget: markdown, required: false }

    - label: Items (Grid / CTA)
      name: items
      widget: list
      required: false
      collapsed: true
      summary: '{{fields.title}}'
      fields:
        - { label: Tiêu đề, name: title, widget: string }
        - { label: Tiêu đề (EN), name: title_en, widget: string, required: false }
        - { label: Mô tả, name: description, widget: markdown, required: false }
        - { label: Mô tả (EN), name: description_en, widget: markdown, required: false }
        - { label: Ảnh/Icon, name: image, widget: image, required: false }
        - { label: Link, name: link, widget: string, required: false }

    - label: Images (Gallery)
      name: images
      widget: list
      required: false
      collapsed: true
      fields:
        - { label: Ảnh, name: image, widget: image }
        - { label: Chú thích, name: caption, widget: string, required: false }
        - { label: Alt text, name: alt_text, widget: string, required: false }

    - { label: CTA Text, name: ctaText, widget: string, required: false }
    - { label: CTA Text EN, name: ctaText_en, widget: string, required: false }
    - { label: CTA Link, name: ctaLink, widget: string, required: false }


# ============================================================
# COLLECTIONS
# ============================================================

collections:

  # ------------------------------------------------------------
  # SETTINGS
  # ------------------------------------------------------------
  - name: settings
    label: ⚙️ Cài đặt
    files:
      - file: content/config.json
        label: Cấu hình chung
        name: site
        format: json
        fields:
          - { label: Tên website, name: siteName, widget: string }
          - { label: URL website, name: siteUrl, widget: string }

          - label: Thương hiệu
            name: brand
            widget: object
            collapsed: true
            fields:
              - { label: Tên, name: name, widget: string }
              - { label: Tagline, name: tagline, widget: string }
              - { label: Logo, name: logo, widget: image }
              - { label: Mô tả, name: description, widget: text, required: false }

          - label: Intro
            name: intro
            widget: object
            collapsed: true
            fields:
              - { label: Bật intro, name: enable, widget: boolean, default: true }
              - { label: Loại, name: type, widget: select, options: [video, gif, image] }
              - { label: Video URL, name: video, widget: string, required: false }
              - { label: GIF, name: gif, widget: image, required: false }
              - { label: Image, name: image, widget: image, required: false }
              - { label: Chú thích, name: caption, widget: string, required: false }
              - { label: Thời lượng, name: duration, widget: number, default: 3 }

          - label: Chủ đề
            name: theme
            widget: object
            collapsed: true
            fields:
              - { label: Màu chính, name: primary, widget: string }
              - { label: Màu phụ, name: accent, widget: string }

  # ------------------------------------------------------------
  # FOOTER
  # ------------------------------------------------------------
  - name: footer
    label: Footer
    files:
      - file: content/footer.json
        name: footer
        label: Footer
        format: json
        fields:
          - { label: Brand name, name: brand_name, widget: string }
          - { label: Logo, name: logo, widget: image, required: false }
          - { label: Mô tả, name: description, widget: text, required: false }
          - { label: Địa chỉ, name: address, widget: string, required: false }
          - { label: Phone, name: phone, widget: string, required: false }
          - { label: Email, name: email, widget: string, required: false }
          - { label: Bản quyền, name: copyright, widget: string }

          - label: Cột liên kết
            name: columns
            widget: list
            fields:
              - { label: Tiêu đề, name: title, widget: string }
              - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
              - label: Links
                name: links
                widget: list
                fields:
                  - { label: Text, name: text, widget: string }
                  - { label: Text EN, name: text_en, widget: string, required: false }
                  - { label: URL, name: url, widget: string }

  # ------------------------------------------------------------
  # SEO
  # ------------------------------------------------------------
  - name: seo
    label: 🔍 SEO
    files:
      - file: content/seo.json
        label: SEO
        name: seo
        format: json
        fields:
          - { label: Trang chủ, name: index, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Giới thiệu, name: about, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Dịch vụ, name: services, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Khoá học, name: courses, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Thành tựu, name: portfolio, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Tuyển dụng, name: careers, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }
          - { label: Liên hệ, name: contact, widget: object, fields: [{label: Title, name: title, widget: string}, {label: Desc, name: description, widget: text}] }

  # ------------------------------------------------------------
  # PAGES
  # ------------------------------------------------------------
  - name: pages
    label: "📄 Các trang nội dung"
    files:
      - file: content/about.json
        label: Giới thiệu
        name: about
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề (EN), name: title_en, widget: string, required: false }
          - { label: Ảnh Cover, name: cover, widget: image }
          - { label: Mô tả, name: description, widget: markdown }
          - { label: Mô tả (EN), name: description_en, widget: markdown, required: false }
          - <<: *sections_config

      - file: content/services.json
        label: Dịch vụ
        name: services
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Ảnh Cover, name: cover, widget: image }
          - <<: *sections_config

      - file: content/portfolio.json
        label: Thành tựu
        name: portfolio
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Ảnh Cover, name: cover, widget: image }
          - <<: *sections_config

      - file: content/contact.json
        label: Liên hệ
        name: contact
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Bản đồ embed, name: map, widget: text, required: false }
          - <<: *sections_config

      # ABOUT
      - file: content/about.json
        label: Giới thiệu
        name: about
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Ảnh cover, name: cover, widget: image }
          - { label: Mô tả, name: intro, widget: markdown }
          - { label: Mô tả EN, name: intro_en, widget: markdown, required: false }
          - <<: *sections_config

      # SERVICES
      - file: content/services.json
        label: Dịch vụ
        name: services
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Mô tả, name: intro, widget: markdown }
          - <<: *sections_config

      # PORTFOLIO
      - file: content/portfolio.json
        label: Thành tựu
        name: portfolio
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Mô tả, name: intro, widget: markdown }
          - <<: *sections_config

      # COURSES
      - file: content/courses.json
        label: Khóa học
        name: courses
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Mô tả, name: intro, widget: markdown }
          - <<: *sections_config

      # CAREERS
      - file: content/careers.json
        label: Tuyển dụng
        name: careers
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Mô tả, name: intro, widget: markdown }
          - <<: *sections_config

      # CONTACT
      - file: content/contact.json
        label: Liên hệ
        name: contact
        format: json
        fields:
          - { label: Tiêu đề, name: title, widget: string }
          - { label: Tiêu đề EN, name: title_en, widget: string, required: false }
          - { label: Thông tin, name: intro, widget: markdown }
          - <<: *sections_config
